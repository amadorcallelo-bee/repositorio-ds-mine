.venv/
.venv-rag/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.ipynb_checkpoints/
.env
.DS_Store
# Seguimiento de experimentos: la base de MLflow, sus artefactos y el store de archivos
# heredado. Son salidas de una corrida, no codigo, y se regeneran ejecutando el notebook.
mlflow.db
mlartifacts/
mlruns/

# El extracto OPUS es dato de negocio, no codigo: vive en data/ y no entra a la historia
# de git. La ruta se resuelve por AURUM_CSV_PATH o por el propio directorio data/.
data/

# Indice local de Chroma del C-2: se reconstruye desde los PDF, no se versiona.
modulo_c/rag_minero/.chroma/
